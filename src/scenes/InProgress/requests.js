export const getQueueData = async () => {
    //const token = process.env.GITHUB_TOKEN;
  //  try {
     // const response = await fetch(`https://github.albertsons.com/api/v3/repos/albertsons/esgh-central-workflow-queue/actions/variables/ALBERTSONS_RUN_QUEUE`, {
   //   headers: {  'Authorization': `Bearer ${token}` }});
  
    //  if (!response.ok) {
        return [];
    //  }
 //     const responseJson = await response.json();
 //     return JSON.parse(responseJson.value);
 //   } catch (error) {
  //    console.log("Failed to fetch data:", error);
  //    return [];
 //   }
  };